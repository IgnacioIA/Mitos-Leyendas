// =========================================================
// GalleryViewport.jsx
// =========================================================

import "../../styles/sections/galeria/GalleryViewport.css";

export default function GalleryViewport({ children }) {

  return (
    <div className="gallery-viewport">
      {children}
    </div>
  );
}